import R from 'ramda'

import {
  FETCH_MOTOS_SUCCESS,
  LOAD_MORE_MOTOS_SUCCESS,
  SEARCH_MOTO
} from 'actionTypes'

const initialState = {
  ids: [],
  search: ''
}

export default (state = initialState, {type, payload}) => {
  switch (type) {
    case FETCH_MOTOS_SUCCESS:
      return R.merge(state, {
        ids: R.pluck('id', payload)
      })
    case LOAD_MORE_MOTOS_SUCCESS:
      const ids = R.pluck('id', payload)
      return R.merge(state, {
        ids: R.concat(ids, state.ids)
      })
    case SEARCH_MOTO:
      return R.merge(state, {
        search: payload
      })

    default:
      return state
  }
}